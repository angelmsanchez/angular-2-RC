import { Route } from '@angular/router';

import { PomodoroComponent } from './index';

export const PomodoroRoutes: Route[] = [
    {
        path: 'pomodoro',
        component: PomodoroComponent
    }
];
